---
import TrophyIcon from '@assets/icons/TrophyIcon.astro';



interface Props {
	title: string;
    link: string;
}

const { title, link } = Astro.props;
---

<a class="card bg-accent rounded-md flex" href={link}>
    <div class="group content p-3 flex flex-row gap-x-2">
        <p class="heading"><TrophyIcon sty="size-4" /></p>
        <p class="heading text-xs font-semibold";>{title}</p>
    </div>
</a>
  
<style>

    .p
    {
        font-family: 'Mona Sans', sans-serif;
    }

    .card {
    position: relative;
    display: flex;
    align-items: center;
    overflow: hidden;
    line-height: 1.6;
    transition: all 2.00s cubic-bezier(0.23, 1, 0.32, 1);
    }

    .content {
    align-items: flex-start;
    color: var(--color-text-primary);
    transition: all 2.00s cubic-bezier(0.23, 1, 0.32, 1);
    }

    .content .heading {
    line-height: 1.3;
    z-index: 1;
    transition: all 2.00s cubic-bezier(0.23, 1, 0.32, 1);
    }

    .card::before {
    content: "";
    position: absolute;
    right: -5.2rem;
    top: -5.2rem;
    width: 10.4rem;
    height: 10.4rem;
    background: var(--color-primary);
    z-index: 0;
    opacity: 0;
    border-radius: 50%;
    transition: all 2.00s cubic-bezier(0.23, 1, 0.32, 1);
    }

    .card::after {
    content: "";
    position: absolute;
    left: -5.2rem;
    bottom: -5.2rem;
    width: 10.4rem;
    height: 10.4rem;
    background: var(--color-primary);
    z-index: 0;
    opacity: 0;
    border-radius: 50%;
    transition: all 2.00s cubic-bezier(0.23, 1, 0.32, 1);
    }

    .card:hover::before {
    scale: 7;
    opacity: 1;
    }

    .card:hover::after {
    scale: 7;
    opacity: 1;
    }

    .card:hover .content .heading {
    color: var(--color-base);
    }

    .card:hover .content .heading {
    color: var(--color-base);
    scale: 1.05;
    }

    .card:hover {
    box-shadow: 0rem 6px 13px rgba(0, 0, 0, 0.1),
        0rem 24px 24px rgba(0, 0, 0, 0.09), 0rem 55px 33px rgba(0, 0, 0, 0.05),
        0rem 97px 39px rgba(0, 0, 0, 0.01), 0rem 152px 43px rgba(0, 0, 0, 0);
    border-color: var(--color-primary);
    scale: 1.05;
    }

</style>